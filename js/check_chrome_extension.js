// check if extension is installed or not
/*  var extensionId = 'aclnciojmkneoikapnobliockgfaabap';
  var img; 
  img = new Image(); 
  img.src = "chrome-extension://" + extensionId + "/news3.png"; 
  img.onload = function() {
    //alert("You have chrome extension installed."); 
	//if user has extension then check if user is logged in through extension or not
	if($.cookie('username') && validate_email($.cookie('username'))){
		return true;
	}else{
		alert('Please login using TimeM Google chrome extension.');
		return false();
	}
  }; 
  img.onerror = function() { 
    //alert("Please install TimeM Chrome extension.");
	window.location.href='chrome-extension-info.html';
	return false;
  };*/